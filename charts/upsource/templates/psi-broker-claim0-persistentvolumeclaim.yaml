apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app-service: psi-broker-claim0
  name: psi-broker-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
