apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
  creationTimestamp: null
  labels:
    app-service: cassandra
  name: cassandra
spec:
  replicas: 1
  selector:
    matchLabels:
      app-service: cassandra
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
      creationTimestamp: null
      labels:
        app-network/upsource-net-name: "true"
        app-service: cassandra
    spec:
      containers:
        - env:
            - name: UPSOURCE_SUBNET
              value: upsource_subnet
            - name: constraint
              value: node==upsource_cassandra_node
          image: upsource_images_prefixjetbrains/upsource-cassandra:upsource_version
          name: cassandra
          ports:
            - containerPort: 9042
          resources: {}
          volumeMounts:
            - mountPath: /var/lib/cassandra
              name: cassandra-claim0
      restartPolicy: Always
      volumes:
        - name: cassandra-claim0
          persistentVolumeClaim:
            claimName: cassandra-claim0
status: {}
